1: get buffer

CASE BUFFER_SIZE = 200;
1. loop through buffer:
* print first row
* keep the remainder of the buffer in static var
* check if sth in static var: if no: print line
        else join(static var, buff(till the new line))
* overwrite the static var